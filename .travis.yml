language: python

python:
  - 3.6

install:
  - pip install -r requirements.txt
  - pip install coveralls

script:
  - py.test --cov=app/tests
branches:
  only:
    -ft-patch-comment-162362702
after_success:
    - coveralls

